<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="kr.co.iei.news.model.dao.NewsDao">
  <select id="selectAllNotice" resultType="notice">
  		select * from 
    		(select rownum as rnum, n.* from 
        		(select 
            		notice_no, title, write_date, view_count
       			 from notice order by notice_no desc
       			 )n
       		)
       	where rnum between #{start} and #{end}
  </select>
  
  <select id="countAllNotice" resultType="int">
  		select count(*) from notice
  </select>
  
  <select id="selectOneNotice" resultType="notice">
  		select * from notice where notice_no = #{noticeNo}
  </select>
  
  <select id="selectTotalNewsCount" resultType="int">
  		select count(*) from news 
  		<if test="tab == 'promotion'">
		where type='프로모션'
		</if>
		<if test="tab == 'event'">
		where type='이벤트'
		</if>
  </select>
  
  <select id="getNewsNo" resultType="int">
  	select news_seq.nextval from dual
  </select>
  
  <insert id="insertNews">
  	insert into news values(#{newsNo}, #{memberNo}, #{type}, #{startDate}, #{endDate}, #{title}, #{content}, to_char(sysdate,'yyyy-mm-dd'), #{image}, null)
  </insert>
  
  <insert id="insertDiscount">
  	insert into discount values(discount_seq.nextval, #{newsNo}, #{productNo}, 
	  	<if test="discountType == 'Percent'">
	  	  	#{discountPrice}, null)
	  	</if>
	  	<if test="discountType == 'Price'">
	  	  	null, #{discountPrice})
	  	</if>
  </insert>

	<select id="selectAllNews"  resultType="news">
		select * from (
  			select * from (
    			select rownum as rnum, n.* from(select * from news order by news_no desc)n) 
		where rnum between #{start} and #{end})
		<if test="tab == 'promotion'">
		where type='프로모션'
		</if>
		<if test="tab == 'event'">
		where type='이벤트'
		</if>
	</select>
	
	<insert id="insertNotice">
		insert into notice values(notice_seq.nextval, #{memberNo}, #{title}, #{content}, to_char(sysdate,'yyyy-mm-dd'), 0)
	</insert>
	
	<insert id="insertNewsLike">
		insert into news_like values(#{memberNo}, #{newsNo})
	</insert>
	
	<delete id="deleteNewsLike">
		delete from news_like where member_no = #{memberNo} and news_no = #{newsNo}
	</delete>
	
	<select id="selectNewsLikeCount" resultType="news">
		select news_no, count(*) as LIKE_COUNT from news_like 
			group by news_no
			having news_no = #{newsNo}
	</select>
	
	<select id="selectNewsisLike" resultType="int">
		select count(*) as "IS_LIKE" from news_like where member_no = #{memberNo} and news_no = #{newsNo}
	</select>
	
	<select id="selectAllProduct" resultType="product">
		select * from product_tbl
	</select>
	
	<select id="selectAllDiscount" resultType="discount">
		select * from discount where news_no = #{newsNo}
	</select>
	
	<select id="selectOneNews" resultType="news">
		select * from news where news_no = #{newsNo}
	</select>
	
	<select id="selectBestProduct" resultType="product">
		select * from product_best_tbl inner join product_tbl using(product_no)
	</select>
	
	<delete id="deleteDiscount">
		delete from discount where news_no = #{newsNo}
	</delete>
	
	<update id = "updateNews">
		update news set member_no = #{memberNo}, type = #{type}, start_date = #{startDate}, end_date = #{endDate}, title = #{title}, content = #{content} where news_no = #{newsNo}
	</update>
	
	<delete id="deleteNews">
		delete from news where news_no = #{newsNo}
	</delete>
	
	<select id="selectAllNewsBanner" resultType="news">
		select * from news
	</select>
	
	<insert id="insertPoster">
		insert into poster values(poster_seq.nextval, null, null, #{newsNo}, #{image})
	</insert>
	
	<select id="selectAllPoster" resultType="poster">
		select * from poster
	</select>
	
	<select id="selectMainPoster" resultType="poster">
		select * from poster where main_yn = 'Y'
	</select>
	
	<select id="selectNewsPoster" resultType="poster">
		select * from poster where news_yn = 'Y'
	</select>
	
	<select id="selectOneNewsModal" resultType="news">
		select * from news where modal_yn = 'Y'
	</select>
	
	<update id="deleteModal">
		update news set modal_yn = null
	</update>
	
	<update id="insertModal">
		update news set modal_yn = 'Y' where news_no = #{newsNo}
	</update>
	
	<update id="insertBannerMain">
		update poster set main_yn = 'Y' where poster_no = #{posterNo}
	</update>
	
	<update id="deleteBannerMain">
		update poster set main_yn = null where poster_no not in (#{posterNo})
	</update>
		
	<update id="insertBannerNews">
		update poster set news_yn = 'Y' where poster_no = #{posterNo}
	</update>
	
	<update id="deleteBannerNews">
		update poster set news_yn = null where poster_no not in (#{posterNo})
	</update>
	
	<delete id="deleteBanner">
		delete from poster where poster_no = #{posterNo}
	</delete>
	
	<delete id="deleteNotice">
		delete from notice where notice_no = #{noticeNo} 
	</delete>
	
	<update id="updateNotice">
		update notice set title = #{title}, content = #{content}, member_no = #{memberNo}  where notice_no = #{noticeNo} 
	</update>
	
	<update id="viewUpdate">
		update notice set view_count = #{viewCount} where notice_no = #{noticeNo}
	</update>
	
</mapper>
